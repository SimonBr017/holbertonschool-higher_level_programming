#!/usr/bin/node

/**
 * script that prints 3 lines: (like 1-multi_languages.js) but by using an array of string and a loop
 */

const arrayOfVar = ['C is fun', 'Python is cool', 'JavaScript is amazing'];
arrayOfVar.forEach(element => console.log(element));
